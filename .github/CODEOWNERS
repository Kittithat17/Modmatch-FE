# Who gets automatically requested for review when a path changes.
# To enable, uncomment a line and replace the handle with a real GitHub username.
# A username that does not exist fails silently: GitHub assigns nobody.

# Repo owner covers everything by default
# *                    @Kittithat17

# Files that break everyone when they break, so worth a dedicated reviewer
# /.github/            @Kittithat17
# /next.config.ts      @Kittithat17
# /package.json        @Kittithat17
